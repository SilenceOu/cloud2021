server:
  port: 80

spring:
  application:
    name: consul-consumer-order
  cloud:
    consul:
      host: 120.25.221.47
      port: 8500
      discovery:
        service-name: ${spring.application.name}
        heartbeat:
          enabled: true
